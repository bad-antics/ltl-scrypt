// examples/demo.ltl — runnable demo for ltl_scrypt

import std::io
use ltl_scrypt as lib

fn main() {
    io::println("ltl_scrypt demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}